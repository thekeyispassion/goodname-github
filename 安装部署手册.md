# 智能取名系统 — 安装部署手册

> 适用于 Linux / macOS / Windows（WSL），Python 3.9+

## 一、环境要求

| 项目 | 要求 |
|:---|:---|
| 操作系统 | Linux / macOS / Windows(WSL) |
| Python | 3.9 或更高 |
| Git | 任意版本 |
| 网络 | 需要访问 DeepSeek API 和 Supabase |

## 二、获取代码

```bash
git clone <仓库地址>
cd goodname
```

## 三、创建虚拟环境

```bash
python3 -m venv myenv
source myenv/bin/activate    # Linux/macOS
# myenv\Scripts\activate     # Windows
```

## 四、安装依赖

```bash
pip install -r requirements.txt
```

## 五、配置密钥

### 5.1 创建 .env 文件

```bash
cp .env.example .env
```

### 5.2 获取 DeepSeek API Key

1. 打开 [platform.deepseek.com](https://platform.deepseek.com/)
2. 注册账号 → 登录
3. 左侧菜单 "API Keys" → "创建 API Key"
4. 复制 Key（以 `sk-` 开头）
5. 填入 `.env`：`DEEPSEEK_API_KEY=sk-你的key`

### 5.3 获取 Supabase 配置

1. 打开 [supabase.com](https://supabase.com/) → 登录
2. 创建新项目（记住数据库密码）
3. 项目 Settings → API
4. 复制 `Project URL` 和 `anon public key`
5. 填入 `.env`：
   ```
   SUPABASE_URL=https://你的项目.supabase.co
   SUPABASE_KEY=你的anon-key
   ```

## 六、初始化数据库

在 Supabase 后台 → SQL Editor，按顺序运行以下 SQL 文件：

| 顺序 | 文件 | 说明 |
|:---:|:---|:---|
| 1 | `database/supabase_schema.sql` | 建表（naming_sessions、candidate_names 等 7 张表） |
| 2 | `database/add_rating_columns.sql` | 用户评分字段（user_rating、user_note） |
| 3 | `database/add_evaluation_table.sql` | AI 评价表（name_evaluations） |
| 4 | `database/disable_evaluation_rls.sql` | 关闭评价表 RLS |

> 注意：每运行完一个文件，检查是否有报错。`IF NOT EXISTS` 保证重复运行安全。

## 七、启动应用

```bash
streamlit run app.py
```

浏览器访问 `http://localhost:8501`

## 八、开发模式

在 `.env` 中设置 `DEV_MODE=true`，启动后自动跳过登录页面，直接进入取名主页（使用虚拟用户 dev@dev.com）。

## 九、云服务器部署（可选）

### 9.1 使用 nohup 后台运行

```bash
nohup streamlit run app.py --server.port 8501 --server.headless true &
```

### 9.2 使用 systemd（推荐）

创建 `/etc/systemd/system/goodname.service`：

```ini
[Unit]
Description=GoodName Streamlit App
After=network.target

[Service]
User=your-user
WorkingDirectory=/path/to/goodname
Environment="PATH=/path/to/goodname/myenv/bin"
ExecStart=/path/to/goodname/myenv/bin/streamlit run app.py --server.port 8501 --server.headless true
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable goodname
sudo systemctl start goodname
```

### 9.3 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }
}
```

## 十、常见问题

| 问题 | 解决方案 |
|:---|:---|
| `streamlit: command not found` | 确认虚拟环境已激活：`source myenv/bin/activate` |
| `Supabase 连接失败` | 检查 `.env` 中 SUPABASE_URL 和 SUPABASE_KEY 是否正确 |
| `DeepSeek API 调用失败` | 检查 DEEPSEEK_API_KEY 是否正确，网络是否通 |
| `注册失败 Database error` | 运行 `database/fix_registration.sql` 清除触发器 |
| `new row violates RLS` | 运行对应表的建表 SQL，确保 RLS 策略已创建或关闭 |
