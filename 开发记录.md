# 取名系统 - 开发记录（Vibe Coding 交付物）

> 📝 **本文档作用**：按照软件服务外包项目要求，记录使用 Vibe Coding 方式开发的完整过程，
> 包括开发环境、提示词、使用的工具等，作为交付成果物之一。

---

## 一、开发环境

| 项目 | 内容 |
|:---|:---|
| **集成开发环境** | Claude Code（命令行版） |
| **操作系统** | Linux (Ubuntu) |
| **代码管理** | Git + GitHub |
| **主要技术** | Python 3.9+, Streamlit, DeepSeek API, SQLite |
| **文档编写** | Typora + Markdown |

### 环境配置过程

```bash
# 1. 初始化项目目录
mkdir -p goodname/goodname
cd goodname/goodname

# 2. 初始化Git仓库
git init

# 3. 创建虚拟环境（可选但推荐）
python -m venv myenv
source myenv/bin/activate

# 4. 安装依赖
pip install streamlit requests

# 5. 创建 .gitignore
echo "myenv/" > .gitignore
echo "*.db" >> .gitignore
echo "__pycache__/" >> .gitignore
echo ".claude/" >> .gitignore
```

---

## 二、使用的 AI 工具 / Skill / MCP

### 2.1 主要交互方式

| 工具 | 用途 |
|:---|:---|
| **Claude Code CLI** | 与 AI 对话编码的主要界面 |
| **Bash 命令执行** | 创建目录、运行文件、查看结果 |
| **文件读写（Read/Write/Edit）** | 创建和修改代码文件 |

### 2.2 使用的 Skill

| Skill 名称 | 用途 |
|:---|:---|
| `code-review` | 审查代码正确性和质量 |
| `verify` | 验证代码更改是否正常工作 |
| `init` | 初始化 CLAUDE.md 项目文档 |
| `fewer-permission-prompts` | 配置常用命令权限，减少弹窗 |

### 2.3 使用的 MCP 工具（Tool）

| 工具 | 用途 |
|:---|:---|
| `Read` | 读取现有的文档和代码文件 |
| `Write` | 创建新文件 |
| `Edit` | 修改现有文件 |
| `Bash` | 执行命令行操作（创建目录、运行程序等） |
| `WebSearch` | 搜索技术资料 |
| `WebFetch` | 获取在线文档内容 |
| `TaskCreate/Update` | 管理开发任务进度 |
| `Agent` | 并行执行多任务（如同时搜索多个技术方案） |
| `EnterPlanMode` | 复杂任务前进入计划模式 |

---

## 三、重要对话与设定记录

> 只记录**重要的对话**（里程碑、技术决策、需求变更），不记日常流水。

### 2026-07-03

| 事项 | 类型 | 摘要 | 产出 |
|:---|:---:|:---|:---|
| 完善数据库设计 | 📐 技术设计 | 分析原有3张表的不足，重新设计难度1的分表方案（3张核心表），并编写完整代码设计教程 | `技术方案.md` |
| 建立开发记录规范 | 📋 规范设定 | 创建 CLAUDE.md 项目规则，设定开发记录.md作为交付物记录重要对话和设定 | `CLAUDE.md`、`开发记录.md` |
| 完整代码实现 | ⚙️ 功能实现 | 按技术方案.md创建全部代码文件，包括 app.py + 数据库模块 + LLM模块 + 解析器 + UI模块 | 共10个代码文件，见下方清单 |

**代码文件清单**：

| 文件 | 说明 | 状态 |
|:---|:---|:---:|
| `app.py` | 主入口，整合所有模块 | ✅ |
| `database/init.py` | 数据库初始化（3张表） | ✅ 已测试 |
| `database/operations.py` | 数据库增删查改操作 | ✅ 已测试 |
| `llm/prompt_builder.py` | 提示词组装（首次生成 + 多轮优化） | ✅ |
| `llm/client.py` | DeepSeek API 调用 | ✅ |
| `utils/parser.py` | JSON解析器（兼容各种格式） | ✅ 已测试 |
| `ui/sidebar.py` | 左侧信息采集面板 | ✅ |
| `ui/chat_area.py` | 右侧对话区域 + 名字卡片展示 | ✅ |
| `requirements.txt` | 依赖清单（streamlit + requests） | ✅ |
| `.gitignore` | Git忽略配置 | ✅ |

**对应提示词**：

```
用户：根据技术文档完成这个项目

AI处理：
- 创建 database/ init.py + operations.py（SQLite 3张表）
- 创建 llm/ prompt_builder.py + client.py（DeepSeek API）
- 创建 utils/ parser.py（AI返回结果解析）
- 创建 ui/ sidebar.py + chat_area.py（Streamlit界面）
- 创建 app.py 整合所有模块为完整的取名系统
- 创建 requirements.txt / .gitignore
- 测试数据库初始化 ✓ 数据库操作 ✓ 解析器 ✓
```

**对应提示词**：

```
# 第1条：完善数据库设计
用户：记住我是大二计算机科学学生，技术能力弱，在开发取名程序，
浏览goodname.md，使我当前完成的工作，软件服务外包.md时项目要求，
根据现有工作成果，完善数据库设计

AI处理：
- 阅读 goodname.md（需求/流程/技术选型/数据流）+ 软件服务外包.md（难度要求）
- 分析原3表设计不足，重新设计难度1数据库（3张核心表）
- 创建 技术方案.md（数据库DDL + 每个模块的完整代码 + 自学注释）
- 创建 开发记录.md 雏形（开发环境/工具/交付清单）
- 简化 goodname.md 中数据库设计部分

# 第2条：建立固定记录规则 → 第3条：纠正记录方式
用户：建立固定任务，在每一次对话都在开发记录中做记录
→ 用户纠正：前5轮是瞎编的，每次记录容易流水账，只记重要对话和设定

AI处理：
- 删除瞎编的对话记录（第1~5轮非真实对话）
- 修改规则：从"每次对话记录"改为"只记录重要对话和设定"
- 更新 CLAUDE.md 和 memory 中的规则
```

---

### 2026-07-05

| 事项 | 类型 | 摘要 | 产出 |
|:---|:---:|:---|:---|
| 修复 SQLite 线程安全问题 | 🐛 Bug修复 | Streamlit多线程重跑导致"SQLite objects created in a thread can only be used in that same thread"，在 sqlite3.connect() 加 check_same_thread=False 解决 | `database/init.py` |
| Streamlit 命令找不到 | 🔧 环境配置 | streamlit安装在虚拟环境myenv中，需先 source myenv/bin/activate 或用完整路径启动 | 环境配置说明 |

**对应提示词**：

```
用户：streamlit run app.py → streamlit：未找到命令
→ 修复：告知虚拟环境需要激活或用完整路径启动

用户：sqlite3.ProgrammingError: SQLite objects created in a thread 
      can only be used in that same thread
→ 修复：database/init.py 中 sqlite3.connect() 添加 check_same_thread=False

用户：这几次对话你记录了吗，特别是刚才发现的bug
→ 处理：补记本次对话到开发记录.md

用户：StreamlitDuplicateElementKey: key='fav_0_2'
→ 修复：ui/chat_area.py 按钮key加消息序号 msg_idx，改为 fav_{msg_idx}_{i}

用户：第二轮没评分/介绍不全，第三轮显示"暂无名字数据"
→ 修复：llm/prompt_builder.py 中 build_refine_prompt 补上完整JSON格式要求 + 详细系统提示 + 完整名字信息

用户：快速连续操作后网页无回响
→ 修复：加 is_processing 锁，处理中隐藏输入框，防止多个请求堆积

用户：选双字却给三字名 + 修改左侧信息后点生成没反应
→ 修复1：提示词中"双字"翻译为"名取2个字"，强调遵守字数
→ 修复2：sidebar.py 修改模式下重置 session_id/messages，重新进入生成流程

用户：名字字数歧义，选什么都是3字，改成总名字字数
→ 修复：sidebar 选项改为"2个字（如：张磊）"/"3个字（如：张致远）"/"4个字（如：张欧阳明）"
→ prompt 映射为"名取1个字/2个字/3个字"传给 AI

用户：避讳字如果是姓氏要提示
→ 修复：sidebar.py 按钮点击时校验避讳字不能包含姓氏

用户：修改信息失效 + 选四字返回三字
→ 修复1：sidebar.py 去掉内部 with st.sidebar，避免双重嵌套导致按钮失效
→ 修复2：app.py中删掉过时注释

用户：FORMAT_REQUIREMENT 改成纯文字后解析失败，退到模糊提取
→ 修复：改回 JSON 示例格式，名字用占位符 "XX" 避免字数冲突

用户：选四字依然返回三字（示例 "XX" 只有2字，AI跟着走）
→ 修复：格式示例改为动态生成，根据用户选择生成 "X" / "XX" / "XXX"

用户：第二次点"开始取名"（修改模式）返回三字
→ 根因：_get_name_char_count 默认返回值 2，name_length 变空值时静默回退到3字
→ 修复：返回 None 让调用方显式兜底 + sidebar 加字数必选校验

### 2026-07-06

| 事项 | 类型 | 摘要 | 产出 |
|:---|:---:|:---|:---|
| 名字字数全面排查 | 🐛 Bug修复 | 分析4层根因（映射不匹配/prompt不足/多轮被带偏/无后端兜底），重构字数映射表+正则容错+复姓支持+pipeline强化提示词+后端校验 | `llm/prompt_builder.py` `app.py` |
| score入库报错 | 🐛 Bug修复 | AI返回score为None，触发数据库CHECK约束，加sanitize钳位处理 | `database/operations.py` |
| API Key安全配置 | 🔧 环境配置 | 创建.env文件+加.gitignore，Key从代码中分离 | `.env` `.gitignore` `llm/client.py` |
| .gitignore完善 | 📋 规范设定 | 补充.env/claude配置排除条目 | `.gitignore` |

**对应提示词**：

```
用户：关于名字字数不对的问题，全区查找还有没有类似的bug（多轮排查）
→ 修复：合并3处重复映射表为_NAME_LENGTH_MAP单一来源，加_get_name_description()
→ 加_validate_name_lengths后端校验，字数不匹配标⚠️

用户（提供详细分析）：问题根因分析……（4层根因+完整修复方案）
→ 修复1：字典键改为简洁值'2个字'/'3个字'/'4个字'+正则容错
→ 修复2：系统提示开头注入具体字数"N个汉字"
→ 修复3：build_refine_prompt加"最高优先级·不可修改规则"
→ 修复4：容错解析支持'双字'/'2字（含姓）'等各种写法+复姓兼容
→ 修复5：validate_names后端兜底过滤

用户：sqlite3.IntegrityError CHECK constraint failed: score
→ 修复：save_names中score做sanitize钳位 max(1,min(100, score or 80))

用户：补充gitignore文件
→ 修复：添加.env到.gitignore，创建.env配置文件分离Key

用户：确认api key
→ 修复：client.py改为从.env→环境变量→代码回退三级读取
```

---

## 四、开发进度记录

### 2026-07-03

| 时间 | 事项 | 状态 |
|:---|:---|:---:|
| 需求调研 | 整理取名需关注的6个方面 | ✅ 完成 |
| 业务流程设计 | 双区域交互 + 多轮对话流程 | ✅ 完成 |
| UI设计 | 新中式极简风格描述 | ✅ 完成 |
| 技术选型 | Python/Streamlit/DeepSeek/SQLite | ✅ 完成 |
| 数据流设计 | 提示词组装器→API→解析器→展示 | ✅ 完成 |
| 数据库设计（难度1） | 3张表结构+DDL | ✅ 完成 |
| 代码架构设计 | 模块划分+核心代码 | ✅ 完成 |
| 项目规则建立 | CLAUDE.md + 开发记录规范 | ✅ 完成 |
| 代码完整实现 | 10个代码文件全部创建并测试 | ✅ 完成 |
| 安装依赖 | streamlit + requests | ✅ 完成 |

### 待完成

| 事项 | 说明 |
|:---|:---|
| 调试优化 | 修改提示词让名字更好听、减少重复 |
| 代码审查 | 使用 code-review skill 检查 |
| Git 提交 | 提交代码到 Gitee |

---

## 五、遇到的问题与解决

| 日期 | 问题 | 解决方案 |
|:---|:---|:---|
| - | - | - |
| 2026-07-05 | SQLite 线程安全错误 | `sqlite3.connect()` 加 `check_same_thread=False` 参数 |
| 2026-07-05 | streamlit 命令找不到 | streamlit在虚拟环境myenv中，需激活环境或用绝对路径运行 |
| 2026-07-05 | 多轮对话后名字不完整/没数据 | build_refine_prompt缺少完整JSON格式要求，AI输出格式乱掉，补上完整格式说明 |
| 2026-07-05 | 快速连续操作网页卡死 | 加 is_processing 锁，处理中隐藏聊天输入框，防止请求堆积 |
| 2026-07-05 | 选"双字"给出三字名 | 提示词中把"双字"明确翻译为"名取2个字"，强调遵守字数 |
| 2026-07-05 | 修改基础信息后点生成没反应 | sidebar.py 中重置 session_id/messages，让修改后重新进入情况A生成 |
| 2026-07-06 | 字典键不匹配导致回退默认字数 | 映射键改为简洁值 + 正则容错 + 复姓兼容 |
| 2026-07-06 | score入库CHECK约束失败 | save_names中score做sanitize钳位处理 |
| 2026-07-06 | API Key可能随代码推送暴露 | .env文件+gitignore分离Key，三级读取机制 |

---

## 六、交付成果物清单

| 成果物 | 文件 | 状态 |
|:---|:---|:---:|
| 需求规格说明书 | goodname.md（需求部分） | ✅ |
| 技术方案 | 技术方案.md | ✅ |
| 源代码 | （待创建） | ⏳ |
| 部署文件 | （待创建） | ⏳ |
| 安装部署手册 | （待创建） | ⏳ |
| 用户操作手册 | （待创建） | ⏳ |
| 项目规则 | CLAUDE.md | ✅ |
| 开发环境记录 | 开发记录.md | ✅ |
| 提示词记录 | 开发记录.md（重要对话部分） | ✅ |
| 使用工具/Skill/MCP记录 | 开发记录.md（工具部分） | ✅ |
