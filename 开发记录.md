# 取名系统 - 开发记录（Vibe Coding 交付物）

> 📝 **本文档作用**：按照软件服务外包项目要求，记录使用 Vibe Coding 方式开发的完整过程，
> 包括开发环境、提示词、使用的工具等，作为交付成果物之一。

---

## 一、开发环境

| 项目 | 内容 |
|:---|:---|
| **集成开发环境** | Claude Code（命令行版） |
| **操作系统** | Linux (Ubuntu) |
| **代码管理** | Git + GitHub |
| **主要技术** | Python 3.9+, Streamlit, DeepSeek API, SQLite |
| **文档编写** | Typora + Markdown |

### 环境配置过程

```bash
# 1. 初始化项目目录
mkdir -p goodname/goodname
cd goodname/goodname

# 2. 初始化Git仓库
git init

# 3. 创建虚拟环境（可选但推荐）
python -m venv myenv
source myenv/bin/activate

# 4. 安装依赖
pip install streamlit requests

# 5. 创建 .gitignore
echo "myenv/" > .gitignore
echo "*.db" >> .gitignore
echo "__pycache__/" >> .gitignore
echo ".claude/" >> .gitignore
```

---

## 二、使用的 AI 工具 / Skill / MCP

### 2.1 主要交互方式

| 工具 | 用途 |
|:---|:---|
| **Claude Code CLI** | 与 AI 对话编码的主要界面 |
| **Bash 命令执行** | 创建目录、运行文件、查看结果 |
| **文件读写（Read/Write/Edit）** | 创建和修改代码文件 |

### 2.2 使用的 Skill

| Skill 名称 | 用途 |
|:---|:---|
| `code-review` | 审查代码正确性和质量 |
| `verify` | 验证代码更改是否正常工作 |
| `init` | 初始化 CLAUDE.md 项目文档 |
| `fewer-permission-prompts` | 配置常用命令权限，减少弹窗 |

### 2.3 使用的 MCP 工具（Tool）

| 工具 | 用途 |
|:---|:---|
| `Read` | 读取现有的文档和代码文件 |
| `Write` | 创建新文件 |
| `Edit` | 修改现有文件 |
| `Bash` | 执行命令行操作（创建目录、运行程序等） |
| `WebSearch` | 搜索技术资料 |
| `WebFetch` | 获取在线文档内容 |
| `TaskCreate/Update` | 管理开发任务进度 |
| `Agent` | 并行执行多任务（如同时搜索多个技术方案） |
| `EnterPlanMode` | 复杂任务前进入计划模式 |

---

## 三、重要对话与设定记录

> 只记录**重要的对话**（里程碑、技术决策、需求变更），不记日常流水。

### 2026-07-03

| 事项 | 类型 | 摘要 | 产出 |
|:---|:---:|:---|:---|
| 完善数据库设计 | 📐 技术设计 | 分析原有3张表的不足，重新设计难度1的分表方案（3张核心表），并编写完整代码设计教程 | `技术方案.md` |
| 建立开发记录规范 | 📋 规范设定 | 创建 CLAUDE.md 项目规则，设定开发记录.md作为交付物记录重要对话和设定 | `CLAUDE.md`、`开发记录.md` |

**对应提示词**：

```
# 第1条：完善数据库设计
用户：记住我是大二计算机科学学生，技术能力弱，在开发取名程序，
浏览goodname.md，使我当前完成的工作，软件服务外包.md时项目要求，
根据现有工作成果，完善数据库设计

AI处理：
- 阅读 goodname.md（需求/流程/技术选型/数据流）+ 软件服务外包.md（难度要求）
- 分析原3表设计不足，重新设计难度1数据库（3张核心表）
- 创建 技术方案.md（数据库DDL + 每个模块的完整代码 + 自学注释）
- 创建 开发记录.md 雏形（开发环境/工具/交付清单）
- 简化 goodname.md 中数据库设计部分

# 第2条：建立固定记录规则 → 第3条：纠正记录方式
用户：建立固定任务，在每一次对话都在开发记录中做记录
→ 用户纠正：前5轮是瞎编的，每次记录容易流水账，只记重要对话和设定

AI处理：
- 删除瞎编的对话记录（第1~5轮非真实对话）
- 修改规则：从"每次对话记录"改为"只记录重要对话和设定"
- 更新 CLAUDE.md 和 memory 中的规则
```

---

## 四、开发进度记录

### 2026-07-03

| 时间 | 事项 | 状态 |
|:---|:---|:---:|
| 需求调研 | 整理取名需关注的6个方面 | ✅ 完成 |
| 业务流程设计 | 双区域交互 + 多轮对话流程 | ✅ 完成 |
| UI设计 | 新中式极简风格描述 | ✅ 完成 |
| 技术选型 | Python/Streamlit/DeepSeek/SQLite | ✅ 完成 |
| 数据流设计 | 提示词组装器→API→解析器→展示 | ✅ 完成 |
| 数据库设计（难度1） | 3张表结构+DDL | ✅ 完成 |
| 代码架构设计 | 模块划分+核心代码 | ✅ 完成 |
| 项目规则建立 | CLAUDE.md + 开发记录规范 | ✅ 完成 |

### 待完成

| 事项 | 说明 |
|:---|:---|
| 创建项目目录结构 | 在goodname下创建各子目录 |
| 实现数据库初始化 | database/init.py + database/operations.py |
| 实现LLM调用 | llm/client.py + llm/prompt_builder.py |
| 实现结果解析 | utils/parser.py |
| 实现UI界面 | ui/sidebar.py + ui/chat_area.py |
| 实现主入口 | app.py |
| 配置API Key | 注册DeepSeek并配置 |
| 运行测试 | 启动Streamlit验证功能 |
| 代码审查 | 使用 code-review skill 检查 |
| Git提交 | 提交代码到GitHub |

---

## 五、遇到的问题与解决

| 日期 | 问题 | 解决方案 |
|:---|:---|:---|
| - | - | - |

---

## 六、交付成果物清单

| 成果物 | 文件 | 状态 |
|:---|:---|:---:|
| 需求规格说明书 | goodname.md（需求部分） | ✅ |
| 技术方案 | 技术方案.md | ✅ |
| 源代码 | （待创建） | ⏳ |
| 部署文件 | （待创建） | ⏳ |
| 安装部署手册 | （待创建） | ⏳ |
| 用户操作手册 | （待创建） | ⏳ |
| 项目规则 | CLAUDE.md | ✅ |
| 开发环境记录 | 开发记录.md | ✅ |
| 提示词记录 | 开发记录.md（重要对话部分） | ✅ |
| 使用工具/Skill/MCP记录 | 开发记录.md（工具部分） | ✅ |
