# 侧边栏（左侧信息采集面板）展示/隐藏逻辑

> ⚠️ **本文件独立存在，专为记录侧边栏的展示/隐藏状态机。**  
> 修改 `pages/1_取名主页.py` 或 `ui/sidebar.py` 时请对照本文件，确保状态逻辑不被破坏。

---

## 1. 核心状态

| 变量 | 类型 | 默认值 | 说明 |
|:---|:---:|:---:|:---|
| `st.session_state.info_submitted` | `bool` | `False` | 用户是否已经提交了表单 |

- `False` → 显示**完整表单**，主区域显示**欢迎页**
- `True` → 显示**摘要 + 可折叠修改区**，主区域显示**对话页**

---

## 2. 状态切换图

```
                   用户点击「开始取名」
                          │
    ┌─────────────────────▼──────────────────────┐
    │         info_submitted = False              │
    │                                             │
    │  侧边栏：render_sidebar() 完整表单           │
    │  主区域：欢迎语「请在左侧填写信息…」          │
    │                                             │
    │  点击按钮后 → sidebar.py 设置                │
    │    st.session_state.info_submitted = True   │
    │    st.session_state.user_input = {…}        │
    └─────────────────────┬──────────────────────┘
                          │
                          ▼
    ┌─────────────────────────────────────────────┐
    │         info_submitted = True               │
    │                                             │
    │  侧边栏：st.info(摘要)                       │
    │         + st.expander("查看/修改信息")       │
    │             └── 点开才显示 render_sidebar()  │
    │                                             │
    │  主区域：render_chat_area() 对话页面         │
    │                                             │
    │  修改信息后 → sidebar.py 重置                │
    │    session_id = None                        │
    │    messages = []                            │
    │    重新触发生成                              │
    └─────────────────────────────────────────────┘
```

---

## 3. 相关代码位置

### 3.1 状态定义 → `pages/1_取名主页.py` 第 68~81 行

```python
for key in ['messages', 'round_number', 'session_id', 'info_submitted',
             'need_process', 'is_processing', 'user_input']:
    if key not in st.session_state:
        if key in ('round_number',):
            st.session_state[key] = 0
        elif key in ('info_submitted', 'need_process', 'is_processing'):
            st.session_state[key] = False
        elif key == 'user_input':
            st.session_state[key] = {}
        elif key == 'session_id':
            st.session_state[key] = None
        else:
            st.session_state[key] = []
```

> 这段兜底初始化保证页面刷新后变量存在（不报错）。

### 3.2 侧边栏展示 → `pages/1_取名主页.py` 第 84~93 行

```python
with st.sidebar:
    st.markdown("## 📝 取名信息采集")
    if st.session_state.info_submitted:
        info = st.session_state.user_input
        st.info(f"👤 {info.get('surname','')}姓 {info.get('gender','')}"
                f"{' | '+info.get('name_length','') if info.get('name_length') else ''}")
        with st.expander("📝 查看/修改信息"):
            render_sidebar()
    else:
        render_sidebar()
```

> `render_sidebar()` 定义在 `ui/sidebar.py`。  
> 它在 `info_submitted=False` 时显示完整表单；  
> 在 `info_submitted=True` 时被包在 `st.expander` 内，默认折叠。

### 3.3 主区域展示 → `pages/1_取名主页.py` 第 96~105 行

```python
if not st.session_state.info_submitted:
    st.markdown("""<div style='text-align:center; padding-top:80px;'>…</div>""",
                unsafe_allow_html=True)
else:
    render_chat_area()
```

### 3.4 状态切换触发 → `ui/sidebar.py` 第 115~131 行

```python
# 用户点击按钮后收集数据
st.session_state.user_input = {
    'surname': surname.strip(),
    'gender': gender,
    # ……
}

# 如果是修改模式（已提交过），重置会话
if st.session_state.get('info_submitted', False):
    st.session_state.session_id = None
    st.session_state.messages = []
    st.session_state.round_number = 0
    st.session_state.need_process = False

# 标记已提交
st.session_state.info_submitted = True
```

---

## 4. 修改注意事项

| 风险操作 | 需要同步检查的位置 |
|:---|:---|
| 修改 `info_submitted` 的默认值 | `pages/1_取名主页.py` L68-81 |
| 修改 `render_sidebar()` 函数 | 确保它不依赖外部 `with st.sidebar` |
| 添加新的侧边栏组件 | 检查 `info_submitted=True` 分支下是否会重复渲染 |
| 修改「开始取名」按钮逻辑 | `ui/sidebar.py` 中 `if clicked:` 块：校验、存数据、切状态 |
| 修改余额不足时的处理 | `pages/1_取名主页.py` L119-124：置 `info_submitted=False` + `st.stop()` |
| 增加新页面 | 如果需要侧边栏，复制 L84-93 的模式 |

---

## 5. 侧边栏 vs Streamlit 原生导航

当前页面（`pages/1_取名主页.py`）在 L19-23 隐藏了 Streamlit 自动生成的 pages 导航：

```python
st.markdown("""
<style>
[data-testid="stSidebarNav"] {display: none !important;}
</style>
""", unsafe_allow_html=True)
```

所以侧边栏中的 `with st.sidebar` 只包含自定义的信息采集面板，不含多页面导航。页面导航放在顶栏。如果要恢复 Streamlit 默认导航，删掉这段 CSS 即可。
